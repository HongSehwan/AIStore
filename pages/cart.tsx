import React from "react";

const CartPage: React.FC = () => {
    return (
        <div>
            <h1>Cart</h1>
            <button>AI MATCHING</button>
        </div>
    );
};

export default CartPage;
